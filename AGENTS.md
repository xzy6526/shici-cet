# CET 四六级背单词项目规则

本文件只补充当前项目的特殊约束；全局规则以 `D:\CodexProjects\AGENTS.md` 为准。

## 项目定位

- 项目是 CET-4 优先、预留 CET-6 的背词网站；
- 核心优先级固定为：背词有效性 ＞ 操作流畅度 ＞ 简洁易用 ＞ UI 美观 ＞ 花哨功能；
- 当前目录是正式项目主路径，禁止在 workspace 或其他目录继续产生另一份主代码。

## 工作边界

- 第一版只做首页、背词未揭晓、背词答案展开、今日完成四个核心状态；
- 词库、搜索、收藏夹、设置等后续能力不得提前扩大为复杂后台；
- 不新增账号、云同步、社区、排行榜、游戏化、AI 老师和提醒系统；
- 未核验的词义、例句、真题出处、频率或版权信息必须明确标注为示例或待确认，不能写成真题数据。

## 代码与文件规则

- 每次开始先读取 `README.md` 和本文件；
- 源码放 `src`，文档放 `docs`，测试放 `tests`，静态资源放 `assets`，临时文件放 `work`；
- 修改前先说明影响文件和范围，保持现有 `server.mjs` 的最小职责；
- 优先使用浏览器原生能力、CSS 和现有 Node 能力，不为简单动效添加重型依赖；
- 不得静默删除或覆盖用户文件，不得改动无关项目。

## 交互与视觉规则

- Mobile First，同时保证桌面端可用；
- 暖白 / 极浅灰实体背景，接近黑色正文，中性灰辅助文字，一个低饱和品牌强调色；
- 学习页保持沉浸，首页主操作必须比辅助数据更突出；
- 新词初次出现只显示单词、发音和“不认识 / 模糊 / 认识”；
- 选择后再渐进揭示释义、例句和考点；用户主动点击“下一个”后才切词；
- 动效优先使用 `transform` 与 `opacity`，必须可中断；
- 遵守 `prefers-reduced-motion`，减少动态时保留清晰状态反馈；
- 所有交互控件需要可键盘操作、可见焦点和中文可理解标签。

## 数据与进度规则

- 学习顺序为“到期复习 → 今日新词 → 当天薄弱词巩固”；
- 每完成一个词立即写入本地状态，刷新后恢复当前队列和位置；
- 调度算法先用可解释的最小实现；只有出现真实需求和测试依据时，才评估 `ts-fsrs`；
- 本地数据不能被网络请求或账号系统依赖；
- 后续接入真题词库前，先记录来源、许可证、字段完整性和可追溯关系。

## 验证要求

- 运行 `npm start` 后检查页面非空、控制台无新增运行时错误；
- 至少验证桌面视口和一个手机视口；
- 验证首页 → 开始背词 → 选择认识程度 → 查看答案 → 下一个 → 今日完成 → 返回首页；
- 验证刷新恢复、快速连续点击、深色模式、减少动态效果；
- 未执行的测试必须写“尚未验证”，不得把服务器启动成功写成产品验收通过。
